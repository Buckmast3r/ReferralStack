import React from 'react';
import Home from './pages/Home'; // Ensure the path matches the file structure

const App = () => {
    return (
        <div>
            <Home />
        </div>
    );
};

export default App;
